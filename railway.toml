[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health"
startCommand = "node server.js"

[[services.ports]]
port = 3000
protocol = "tcp"
# 👇 ВАЖНО: разрешить WebSocket
handlers = ["http", "websocket"]
